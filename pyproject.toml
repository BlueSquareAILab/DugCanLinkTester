[project]
name = "dugcanlinktester"
version = "0.1.0"
description = "J1939 CAN Joystick Monitor with PySide6 GUI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.6",
    "pyserial>=3.5",
]

[project.scripts]
dugcanlinktester = "main:main"
